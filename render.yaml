services:
  - type: web
    name: ebbinghaus-bot
    env: python
    plan: free
    python:
      version: "3.11.9"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    
